import { expect } from "chai";

describe('stale branches', () => {
    it('should contain more than just a dummy spec, delete this and introduce better tests!', () => {
        expect(1).to.equal(1);
    });
});
